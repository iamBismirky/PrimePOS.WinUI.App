<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PrimePOS.WinUI.Overlays.ClienteOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimePOS.WinUI.Overlays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Visibility="{Binding IsOverlayVisible}"
      Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
      Opacity="0.8">

        <Border Width="400"
            Padding="20"
            Background="{ThemeResource CardStrokeColorDefaultSolidBrush}"
            CornerRadius="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <StackPanel Spacing="10">

                <TextBlock Text="Cliente"
                       FontSize="18"
                       FontWeight="SemiBold"/>

                <Line X1="0" X2="1" Stretch="Fill" Stroke="Gray" StrokeThickness="1"/>

                <StackPanel Orientation="Horizontal" Spacing="50" HorizontalAlignment="Stretch">
                    <TextBlock>
                    <Run Text="Codigo"/>
                    <Run Text="{Binding Codigo}"/>
                    </TextBlock>
                    <TextBlock>
                    <Run Text="Fecha Registro"/>
                    <Run Text="{Binding FechaRegistro, Converter={StaticResource DateFormatConverter}}"/>
                    </TextBlock>
                </StackPanel>


                <Line X1="0" X2="1" Stretch="Fill" Stroke="Gray" StrokeThickness="1"/>
                <TextBox Header="Nombre" Text="{Binding Nombre, Mode=TwoWay}"/>
                <TextBox Header="Documento" Text="{Binding Documento, Mode=TwoWay}"/>
                <TextBox Header="Telefono" Text="{Binding Telefono, Mode=TwoWay}"/>
                <TextBox Header="Email" Text="{Binding Email, Mode=TwoWay}"/>
                <TextBox Header="Direccion" Text="{Binding Direccion, Mode=TwoWay}"/>
                <ToggleSwitch Header="Estado" IsOn="{Binding Estado, Mode=TwoWay}"/>

                <Line X1="0" X2="1" Stretch="Fill" Stroke="Gray" StrokeThickness="1"/>

                <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Spacing="10">

                    <Button Command="{Binding GuardarCommand}">
                        
                        <TextBlock Text="Guardar"/>
                    </Button>

                    <Button Command="{Binding CancelarCommand}">
                        <TextBlock Text="Cancelar"/>
                    </Button>

                </StackPanel>

            </StackPanel>

        </Border>

    </Grid>
</UserControl>
