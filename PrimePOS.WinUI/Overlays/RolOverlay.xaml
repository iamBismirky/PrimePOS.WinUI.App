<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PrimePOS.WinUI.Overlays.RolOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimePOS.WinUI.Overlays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:PrimePOS.WinUI.Helpers"
    mc:Ignorable="d">

    <UserControl.Resources>
        <helpers:NumeroConverterHelper x:Key="NumeroConverterHelper"/>
    </UserControl.Resources>
    <Grid >

        <Border Width="400"
            Padding="20"
            Background="{ThemeResource CardStrokeColorDefaultSolidBrush}"
            CornerRadius="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <StackPanel Spacing="10">

                <TextBlock x:Name ="Rol"
                           FontSize="18"
                           FontWeight="SemiBold"/>
                <!-- Nombre -->
                <TextBox Text="{Binding rolSeleccionado.Nombre}"
                         Header="Nombre"
                         Grid.Row="0"
                         Grid.Column="0"/>



                <!-- BOTONES -->
                <StackPanel Orientation="Horizontal"
                            Spacing="10"
                            HorizontalAlignment="Right">

                    <!--<Button Content="Cancelar"
                            Click="BtnCerrar_Click" />

                    <Button Content="Guardar"
                            Click="Guardar_Click" />-->


                </StackPanel>

            </StackPanel>

        </Border>

    </Grid>
</UserControl>
