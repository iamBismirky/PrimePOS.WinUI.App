<UserControl
    x:Class="PrimePOS.WinUI.Overlays.AbrirTurnoOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>

        <Border Width="400"
                Padding="20"
                Background="{ThemeResource CardStrokeColorDefaultSolidBrush}"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel Spacing="10">

                <TextBlock Text="Abrir Turno"
                           FontSize="22"
                           FontWeight="Bold"/>

                <TextBlock Text="{Binding TurnoTexto}" />

                <ComboBox ItemsSource="{Binding Cajas}"
                          SelectedItem="{Binding CajaSeleccionada}"
                          DisplayMemberPath="Nombre"/>

                <NumberBox Header="Monto Inicial"
                           Value="{Binding MontoInicial, Mode=TwoWay}" />

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Cancelar"
                            Command="{Binding CancelarCommand}" />
                    <Button Content="Abrir"
                            Command="{Binding AbrirCommand}" />
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>