<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimePOS.WinUI.Pages.ProductoPageVM"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimePOS.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid RowSpacing="15">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Gestion de Productos" Grid.Row="0" FontWeight="SemiBold" FontSize="24"/>
            <!-- BUSCADOR -->
            <Border Padding="15"
                CornerRadius="12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Height="100"
                    Grid.Row="1">

                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    
                        <FontIcon Glyph="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="18"/>
                    
                    <AutoSuggestBox 
                                    PlaceholderText="Buscar producto..."
                                    TextChanged="TxtBuscar_TextChanged"
                                    QuerySubmitted="TxtBuscar_QuerySubmitted"
                                    SuggestionChosen="AutoSuggest_SuggestionChosen"
                                    Width="300"/>
                    
                    <Button 
                            HorizontalAlignment="Right"
                            Click="BtnAgregarProducto_Click">
                        <SymbolIcon Symbol="Add"/>
                    </Button>
                    

                </StackPanel>
                


            </Border>

            <!-- 📋 GRID -->
            <Border Grid.Row="2"
                Padding="15"
                CornerRadius="12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                <StackPanel Spacing="10">

                    <!-- HEADER -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <TextBlock Text="Productos"
                               FontSize="20"
                               FontWeight="SemiBold"/>

                        
                    </StackPanel>

                    <!-- DATA GRID -->
                    <controls:DataGrid x:Name="dgProductos"
                                       AutoGenerateColumns="False">

                        <controls:DataGrid.Columns>

                            <controls:DataGridTextColumn Header="Codigo" Binding="{Binding Codigo}"/>
                            <controls:DataGridTextColumn Header="Codigo de Barras" Binding="{Binding CodigoBarra}"/>
                            <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"/>
                            <controls:DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}"/>
                            <controls:DataGridTextColumn Header="Categoria" Binding="{Binding NombreCategoria}"/>
                            <controls:DataGridTextColumn Header="Precio Compra" Binding="{Binding PrecioCompra}"/>
                            <controls:DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta}"/>
                            <controls:DataGridTextColumn Header="Existencia" Binding="{Binding Existencia}"/>
                            <controls:DataGridTextColumn Header="Fecha de Registro" Binding="{Binding FechaRegistro}"/>

                            <!-- 🔥 ACCIONES -->
                            <controls:DataGridTemplateColumn Header="Acciones">
                                <controls:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <StackPanel Orientation="Horizontal" Spacing="5">

                                            <Button Click="BtnEditar_Click">

                                                <SymbolIcon Symbol="View"/>
                                            </Button>
                                            <Button 
                                                    Click="BtnEliminar_Click">
                                                <SymbolIcon Symbol="Delete"/>
                                            </Button>

                                        </StackPanel>

                                    </DataTemplate>
                                </controls:DataGridTemplateColumn.CellTemplate>
                            </controls:DataGridTemplateColumn>

                        </controls:DataGrid.Columns>

                    </controls:DataGrid>

                </StackPanel>

            </Border>

        </Grid>

        
        <Grid x:Name="OverlayContainer"
          Background="#66000000"
          Visibility="Collapsed"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"/>

        <ContentControl x:Name="OverlayContent"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>


    </Grid>
</Page>
