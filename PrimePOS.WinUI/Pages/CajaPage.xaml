<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimePOS.WinUI.Pages.CajaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimePOS.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="20" RowSpacing="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- CARD FORMULARIO -->
        <Border Grid.Row="0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="12"
            Padding="20">

            <StackPanel Spacing="15">

                <TextBlock Text="Gestión de Cajas"
                        FontSize="24"
                        FontWeight="SemiBold"/>

                <Grid ColumnSpacing="15" RowSpacing="10">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>




                    <!-- Nombre -->
                    <TextBox x:Name="txtNombre"
                 Header="Nombre"
                 Grid.Row="0"
                 Grid.Column="0"/>

                    
                    <!-- Estado -->
                    <ToggleSwitch x:Name="tsEstado"
                              Header="Estado"
                              IsOn="True"
                              OnContent="Activo"
                              OffContent="Inactivo"
                              Grid.Row="0"
                              Grid.Column="1"/>

                </Grid>


                <!-- BOTONES -->
                <StackPanel Orientation="Horizontal" Spacing="10">

                    <Button Click="BtnCrear_Click">

                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <SymbolIcon Symbol="Add"/>
                            <TextBlock Text="Crear"/>
                        </StackPanel>

                    </Button>

                    <Button Click="BtnActualizar_Click">

                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <SymbolIcon Symbol="Edit"/>
                            <TextBlock Text="Actualizar"/>
                        </StackPanel>

                    </Button>

                    <Button Click="BtnEliminar_Click">

                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <SymbolIcon Symbol="Delete"/>
                            <TextBlock Text="Eliminar"/>
                        </StackPanel>

                    </Button>

                    <Button Click="BtnLimpiar_Click">

                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <SymbolIcon Symbol="Refresh"/>
                            <TextBlock Text="Limpiar"/>
                        </StackPanel>

                    </Button>

                </StackPanel>

            </StackPanel>

        </Border>



        <!-- CARD DATAGRID -->
        <Border Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="12"
            Padding="20">

            <StackPanel Spacing="10">

                <TextBlock Text="Lista de Cajas"
                       FontSize="18"
                       FontWeight="SemiBold"/>

                <controls:DataGrid x:Name="dgCajas"
                               AutoGenerateColumns="False"
                               SelectionChanged="dgCajas_SelectionChanged"
                               SelectionMode="Extended"
                               IsReadOnly="True"
                               IsTabStop="false">

                    <controls:DataGrid.Columns>

                        <controls:DataGridTextColumn Header="Nombre"
                                                 Binding="{Binding Nombre}"
                                                 Width="auto"/>


                        <controls:DataGridCheckBoxColumn Header="Estado"
                                                 Binding="{Binding Estado}"
                                                 Width="auto"/>

                         
                    </controls:DataGrid.Columns>

                </controls:DataGrid>

            </StackPanel>

        </Border>

    </Grid>
</Page>
