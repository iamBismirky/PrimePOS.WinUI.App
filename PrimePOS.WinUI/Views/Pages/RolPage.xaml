<Page
    x:Class="PrimePOS.WinUI.Views.Pages.RolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid
        x:Name="MainRoot"
        Padding="25"
        RowSpacing="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  =====================================  -->
        <!--  HEADER  -->
        <!--  =====================================  -->

        <Border Padding="10,5,10,10">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="5">

                    <TextBlock
                        FontSize="32"
                        FontWeight="Bold"
                        Text="Roles" />

                    <TextBlock Opacity="0.6" Text="Control de permisos y acceso al sistema" />

                </StackPanel>

                <Button
                    Grid.Column="1"
                    Padding="25,10"
                    Background="#2D89EF"
                    Command="{Binding NuevoCommand}"
                    Foreground="White">

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" />
                        <TextBlock Text="Nuevo Rol" />
                    </StackPanel>

                </Button>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  SEARCH  -->
        <!--  =====================================  -->

        <Border
            Grid.Row="1"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="14">

            <Grid ColumnSpacing="15">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox PlaceholderText="Buscar rol por nombre..." Text="{Binding Buscar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="1" Command="{Binding FiltrarCommand}">
                    <FontIcon FontSize="18" Glyph="&#xE721;" />
                </Button>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  GRID  -->
        <!--  =====================================  -->

        <Border
            Grid.Row="2"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="16">

            <Grid RowSpacing="10">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  HEADER GRID  -->
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Text="Roles" />

                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Opacity="0.6"
                        Text="{Binding Roles.Count}" />

                </Grid>

                <!--  TABLE  -->
                <controls:DataGrid
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    GridLinesVisibility="None"
                    IsReadOnly="True"
                    ItemsSource="{Binding Roles}"
                    RowHeight="45">

                    <controls:DataGrid.Columns>

                        <controls:DataGridTextColumn Binding="{Binding Nombre}" Header="Nombre" />

                        <controls:DataGridCheckBoxColumn Binding="{Binding Estado}" Header="Estado" />

                        <!--  ACCIONES  -->
                        <controls:DataGridTemplateColumn Header="">

                            <controls:DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <StackPanel Orientation="Horizontal" Spacing="6">

                                        <!--  EDITAR  -->
                                        <Button
                                            Background="#2D89EF"
                                            Click="Editar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE70F;" />
                                        </Button>

                                        <!--  DESACTIVAR  -->
                                        <Button
                                            Background="#C42B1C"
                                            Click="Desactivar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE74D;" />
                                        </Button>

                                    </StackPanel>

                                </DataTemplate>

                            </controls:DataGridTemplateColumn.CellTemplate>

                        </controls:DataGridTemplateColumn>

                    </controls:DataGrid.Columns>

                </controls:DataGrid>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  LOADING  -->
        <!--  =====================================  -->

        <Grid
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            Opacity="0.85"
            Visibility="{Binding IsLoading}">

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10">

                <ProgressRing
                    Width="50"
                    Height="50"
                    IsActive="True" />

                <TextBlock Opacity="0.7" Text="Cargando roles..." />

            </StackPanel>

        </Grid>

    </Grid>

</Page>