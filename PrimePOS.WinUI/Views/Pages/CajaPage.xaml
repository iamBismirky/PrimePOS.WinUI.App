<Page
    x:Class="PrimePOS.WinUI.Views.Pages.CajaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid
        x:Name="MainRoot"
        Padding="25"
        RowSpacing="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  =====================================  -->
        <!--  HEADER MODERNO  -->
        <!--  =====================================  -->

        <Border Padding="10,5,10,10">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="5">

                    <TextBlock
                        FontSize="32"
                        FontWeight="Bold"
                        Text="Cajas" />

                    <TextBlock Opacity="0.6" Text="Administra las cajas del sistema POS" />

                </StackPanel>

                <Button
                    Grid.Column="1"
                    Padding="25,10"
                    Background="{StaticResource PrimePOSPrimaryBrush}"
                    Command="{Binding NuevoCommand}"
                    Foreground="White">

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" />
                        <TextBlock Text="Nueva Caja" />
                    </StackPanel>

                </Button>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  SEARCH BAR  -->
        <!--  =====================================  -->

        <Border
            Grid.Row="1"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="14">

            <Grid ColumnSpacing="15">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox PlaceholderText="Buscar caja por nombre..." Text="{Binding Buscar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="1" Command="{Binding FiltrarCommand}">
                    <FontIcon FontSize="18" Glyph="&#xE721;" />
                </Button>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  GRID MODERNO  -->
        <!--  =====================================  -->

        <Border
            Grid.Row="2"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="16">

            <Grid RowSpacing="10">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  HEADER GRID  -->
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Text="Cajas" />

                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Opacity="0.6"
                        Text="{Binding Cajas.Count}" />

                </Grid>

                <!--  DATA GRID  -->
                <controls:DataGrid
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    GridLinesVisibility="None"
                    IsReadOnly="True"
                    ItemsSource="{Binding Cajas}"
                    RowHeight="45">

                    <controls:DataGrid.Columns>

                        <controls:DataGridTextColumn Binding="{Binding Nombre}" Header="Nombre" />

                        <controls:DataGridCheckBoxColumn Binding="{Binding Estado}" Header="Estado" />

                        <!--  ACCIONES  -->
                        <controls:DataGridTemplateColumn Header="">

                            <controls:DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <StackPanel Orientation="Horizontal" Spacing="6">

                                        <!--  EDITAR  -->
                                        <Button
                                            Background="{StaticResource PrimePOSPrimaryBrush}"
                                            Click="Editar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE70F;" />
                                        </Button>

                                        <!--  DESACTIVAR  -->
                                        <Button
                                            Background="{StaticResource PrimePOSDangerBrush}"
                                            Click="Desactivar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE74D;" />
                                        </Button>

                                    </StackPanel>

                                </DataTemplate>

                            </controls:DataGridTemplateColumn.CellTemplate>

                        </controls:DataGridTemplateColumn>

                    </controls:DataGrid.Columns>

                </controls:DataGrid>

            </Grid>

        </Border>

        <!--  =====================================  -->
        <!--  LOADING OVERLAY  -->
        <!--  =====================================  -->

        <Grid
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            Opacity="0.85"
            Visibility="{Binding IsLoading}">

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10">

                <ProgressRing
                    Width="50"
                    Height="50"
                    IsActive="True" />

                <TextBlock Opacity="0.7" Text="Cargando cajas..." />

            </StackPanel>

        </Grid>

    </Grid>

</Page>