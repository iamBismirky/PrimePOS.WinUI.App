<Page
    x:Class="PrimePOS.WinUI.Views.Pages.ProductoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid
        x:Name="MainRoot"
        Padding="25"
        RowSpacing="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  =================================================  -->
        <!--  HEADER MODERNO  -->
        <!--  =================================================  -->

        <Border Padding="10,5,10,10">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="5">

                    <TextBlock
                        FontSize="32"
                        FontWeight="Bold"
                        Text="Productos" />

                    <TextBlock Opacity="0.6" Text="Gestiona inventario, precios y disponibilidad" />

                </StackPanel>

                <Button
                    Grid.Column="1"
                    Padding="25,10"
                    Background="{StaticResource PrimePOSPrimaryBrush}"
                    Command="{Binding NuevoCommand}"
                    Foreground="White">

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" />
                        <TextBlock Text="Nuevo" />
                    </StackPanel>

                </Button>

            </Grid>

        </Border>

        <!--  =================================================  -->
        <!--  TOOLBAR (SEARCH)  -->
        <!--  =================================================  -->

        <Border
            Grid.Row="1"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="14">

            <Grid ColumnSpacing="15">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox PlaceholderText="Buscar productos por código, nombre o categoría..." Text="{Binding Buscar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="1" Command="{Binding FiltrarCommand}">
                    <FontIcon FontSize="18" Glyph="&#xE721;" />
                </Button>

            </Grid>

        </Border>

        <!--  =================================================  -->
        <!--  DATA GRID MODERNO  -->
        <!--  =================================================  -->

        <Border
            Grid.Row="2"
            Padding="15"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="16">

            <Grid RowSpacing="10">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  HEADER GRID  -->
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Text="Productos" />

                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Opacity="0.6"
                        Text="{Binding Productos.Count}" />

                </Grid>

                <!--  TABLE  -->
                <controls:DataGrid
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    GridLinesVisibility="None"
                    IsReadOnly="True"
                    ItemsSource="{Binding Productos}"
                    RowHeight="45"
                    SelectionMode="Single">

                    <controls:DataGrid.Columns>

                        <controls:DataGridTextColumn Binding="{Binding Codigo}" Header="Código" />
                        <controls:DataGridTextColumn Binding="{Binding CodigoBarra}" Header="Barra" />
                        <controls:DataGridTextColumn Binding="{Binding Nombre}" Header="Nombre" />
                        <controls:DataGridTextColumn Binding="{Binding Descripcion}" Header="Descripcion" />
                        <controls:DataGridTextColumn Binding="{Binding CategoriaNombre}" Header="Categoría" />

                        <controls:DataGridTextColumn Binding="{Binding PrecioMinorista, Converter={StaticResource NumeroConverterHelper}}" Header="Minorista" />

                        <controls:DataGridTextColumn Binding="{Binding PrecioMayorista, Converter={StaticResource NumeroConverterHelper}}" Header="Mayorista" />

                        <controls:DataGridTextColumn Binding="{Binding Existencia}" Header="Stock" />

                        <controls:DataGridCheckBoxColumn Binding="{Binding Estado}" Header="Activo" Width="100"/>

                        <!--  ACCIONES  -->
                        <controls:DataGridTemplateColumn Header="">

                            <controls:DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <StackPanel Orientation="Horizontal" Spacing="6">

                                        <!--  EDITAR  -->
                                        <Button
                                            Background="{StaticResource PrimePOSPrimaryBrush}"
                                            Click="Editar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE70F;" />
                                        </Button>

                                        <!--  ETIQUETA  -->
                                        <Button Click="VerEtiqueta_Click" Tag="{Binding}">
                                            <FontIcon Glyph="&#xE8A5;" />
                                        </Button>

                                        <!--  DESACTIVAR  -->
                                        <Button
                                            Background="{StaticResource PrimePOSDangerBrush}"
                                            Click="Desactivar_Click"
                                            Tag="{Binding}">
                                            <FontIcon Foreground="White" Glyph="&#xE74D;" />
                                        </Button>

                                    </StackPanel>

                                </DataTemplate>

                            </controls:DataGridTemplateColumn.CellTemplate>

                        </controls:DataGridTemplateColumn>

                    </controls:DataGrid.Columns>

                </controls:DataGrid>

            </Grid>

        </Border>

        <!--  =================================================  -->
        <!--  LOADING OVERLAY  -->
        <!--  =================================================  -->

        <Grid
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            Opacity="0.85"
            Visibility="{Binding IsLoading}">

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10">

                <ProgressRing
                    Width="50"
                    Height="50"
                    IsActive="True" />

                <TextBlock
                    HorizontalAlignment="Center"
                    Opacity="0.7"
                    Text="Cargando productos..." />

            </StackPanel>

        </Grid>

    </Grid>
</Page>