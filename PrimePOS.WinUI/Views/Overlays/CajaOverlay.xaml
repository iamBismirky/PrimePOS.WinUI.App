<UserControl
    x:Class="PrimePOS.WinUI.Views.Overlays.CajaOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Border
            Width="550"
            Padding="25"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{ThemeResource SolidBackgroundFillColorBase}"
            CornerRadius="18">

            <Grid RowSpacing="20">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  ================= HEADER =================  -->
                <StackPanel Grid.Row="0" Spacing="5">

                    <TextBlock
                        FontSize="30"
                        FontWeight="Bold"
                        Text="Caja" />

                    <TextBlock Opacity="0.6" Text="Gestión de caja del sistema" />

                </StackPanel>

                <!--  ================= NOMBRE =================  -->
                <Border
                    Grid.Row="1"
                    Padding="15"
                    Background="{ThemeResource CardStrokeColorDefaultSolidBrush}"
                    CornerRadius="14">

                    <StackPanel Spacing="10">

                        <TextBlock
                            FontWeight="SemiBold"
                            Opacity="0.7"
                            Text="Nombre de la caja" />

                        <TextBox Text="{Binding Nombre, Mode=TwoWay}" />

                    </StackPanel>

                </Border>

                <!--  ================= ESTADO =================  -->
                <Border
                    Grid.Row="2"
                    Padding="15"
                    Background="{ThemeResource CardStrokeColorDefaultSolidBrush}"
                    CornerRadius="14">

                    <StackPanel
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="10">

                        <StackPanel Spacing="5">

                            <TextBlock
                                FontWeight="SemiBold"
                                Opacity="0.7"
                                Text="Estado" />

                            <TextBlock
                                FontSize="12"
                                Opacity="0.5"
                                Text="Activa / Inactiva" />

                        </StackPanel>

                        <ToggleSwitch IsOn="{Binding Estado, Mode=TwoWay}" />

                    </StackPanel>

                </Border>

                <!--  ================= BOTONES =================  -->
                <StackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Spacing="12">

                    <Button
                        Padding="25,10"
                        Command="{Binding CancelarCommand}"
                        Content="Cancelar"
                        Style="{StaticResource SubtleButtonStyle}" />

                    <Grid>

                        <Button
                            Padding="30,10"
                            Background="#FF2D89EF"
                            Command="{Binding GuardarCommand}"
                            Content="Guardar"
                            Foreground="White" />

                        <ProgressRing
                            Width="22"
                            Height="22"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="{Binding IsLoading}" />

                    </Grid>

                </StackPanel>

            </Grid>

        </Border>

    </Grid>

</UserControl>